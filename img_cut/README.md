# Image Crop Tool

把需要处理的图片放到 `input` 文件夹中，然后运行：

```powershell
python crop_images.py
```

处理后的图片会输出到 `output` 文件夹。

裁剪规则：

- 保留图片上方区域，把底部裁掉，目标高度为 `670px`
- 左右居中裁剪，目标宽度为 `670px`
- 宽或高如果本身小于目标值，也会继续处理另一维；不足的那一维保留原尺寸，不跳过整张图

处理完成后，图片会输出到 `output` 文件夹，并在终端显示本次图片是否有某一维保留了原尺寸。
